<nav class="navbar navbar-default">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">MyApp</a>
    <ul class="nav navbar-nav navbar-right">
      <li class="new-story-button"><%= link_to "Write a story", new_post_path, class: "button" %></li>
      <% if user_signed_in? %>
        <li class="current-user-avatar"><%= link_to(avatar_for(current_user, size: 35), current_user) %></li>
        <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li><%= link_to "Sign in / Sign up", new_user_session_path, class: "button" %></li>
      <% end %>
    </ul>
  </div>
</nav>